<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkforge.analytics.infrastructure.persistence.mapper.LinkVisitEventMapper">

    <insert id="batchInsertIgnore">
        INSERT INTO link_visit_events (
            id, tenant_id, link_id, occurred_at, request_id,
            ip_hash, ua_raw, ua_family, os_family, device_type,
            referer_domain, language, utm_source, utm_medium, utm_campaign,
            created_at
        ) VALUES
        <foreach collection="list" item="r" separator=",">
            (
                #{r.id}, #{r.tenantId}, #{r.linkId}, #{r.occurredAt}, #{r.requestId},
                #{r.ipHash}, #{r.uaRaw}, #{r.uaFamily}, #{r.osFamily}, #{r.deviceType},
                #{r.refererDomain}, #{r.language}, #{r.utmSource}, #{r.utmMedium}, #{r.utmCampaign},
                NOW()
            )
        </foreach>
        ON DUPLICATE KEY UPDATE id = id
    </insert>

    <delete id="deleteOld">
        DELETE FROM link_visit_events
        WHERE created_at &lt; DATE_SUB(NOW(), INTERVAL #{param1} DAY)
        LIMIT 5000
    </delete>
</mapper>

