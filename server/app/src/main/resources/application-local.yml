spring:
  flyway:
    url: jdbc:mysql://localhost:3306/linkforge?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    user: linkforge
    password: linkforge
  data:
    redis:
      host: localhost
      port: 6379

app:
  base-url: http://localhost
  security:
    jwt:
      secret: dev-change-me-please-set-env-and-long-enough-32-bytes
