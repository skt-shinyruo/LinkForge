<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkforge.governance.infrastructure.persistence.mapper.ApprovalRequestMapper">

    <resultMap id="ApprovalRequestResultMap" type="com.linkforge.governance.infrastructure.persistence.entity.ApprovalRequestEntity">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="operationType" column="operation_type"/>
        <result property="targetApplicationId" column="target_application_id"/>
        <result property="requestedByUserId" column="requested_by_user_id"/>
        <result property="requestedByEmail" column="requested_by_email"/>
        <result property="status" column="status"/>
        <result property="approverUserId" column="approver_user_id"/>
        <result property="approverEmail" column="approver_email"/>
        <result property="decisionReason" column="decision_reason"/>
        <result property="beforeSnapshot" column="before_snapshot"/>
        <result property="afterSnapshot" column="after_snapshot"/>
        <result property="createdAt" column="created_at"/>
        <result property="decidedAt" column="decided_at"/>
        <result property="executedAt" column="executed_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.linkforge.governance.infrastructure.persistence.entity.ApprovalRequestEntity">
        INSERT INTO approval_requests (
            id,
            tenant_id,
            operation_type,
            target_application_id,
            requested_by_user_id,
            requested_by_email,
            status,
            approver_user_id,
            approver_email,
            decision_reason,
            before_snapshot,
            after_snapshot,
            created_at,
            decided_at,
            executed_at
        ) VALUES (
            #{id},
            #{tenantId},
            #{operationType},
            #{targetApplicationId},
            #{requestedByUserId},
            #{requestedByEmail},
            #{status},
            #{approverUserId},
            #{approverEmail},
            #{decisionReason},
            #{beforeSnapshot},
            #{afterSnapshot},
            #{createdAt},
            #{decidedAt},
            #{executedAt}
        )
    </insert>

    <select id="findByTenantIdAndId" resultMap="ApprovalRequestResultMap">
        SELECT id, tenant_id, operation_type, target_application_id, requested_by_user_id, requested_by_email, status,
               approver_user_id, approver_email, decision_reason, before_snapshot, after_snapshot, created_at,
               decided_at, executed_at
        FROM approval_requests
        WHERE tenant_id = #{tenantId}
          AND id = #{requestId}
    </select>

    <select id="listByTenantId" resultMap="ApprovalRequestResultMap">
        SELECT id, tenant_id, operation_type, target_application_id, requested_by_user_id, requested_by_email, status,
               approver_user_id, approver_email, decision_reason, before_snapshot, after_snapshot, created_at,
               decided_at, executed_at
        FROM approval_requests
        WHERE tenant_id = #{tenantId}
        ORDER BY created_at DESC, id DESC
    </select>

    <!--
        审批认领的并发边界：唯一执行器已由应用层在进入此语句前校验，本语句只按
        tenant_id + id + PENDING_APPROVAL 原子抢占。零行更新表示目标不存在、跨租户或状态已变化。
    -->
    <update id="markApprovedIfPending">
        UPDATE approval_requests
        SET status = 'APPROVED',
            approver_user_id = #{approverUserId},
            approver_email = #{approverEmail},
            decision_reason = #{decisionReason},
            decided_at = #{decidedAt},
            executed_at = NULL
        WHERE tenant_id = #{tenantId}
          AND id = #{requestId}
          AND status = 'PENDING_APPROVAL'
    </update>

    <!--
        仅在执行器成功返回后推进状态。零行更新不是幂等成功，调用方会将其视为执行状态记录失败并回滚事务。
    -->
    <update id="markExecutedIfApproved">
        UPDATE approval_requests
        SET status = 'EXECUTED',
            executed_at = #{executedAt}
        WHERE tenant_id = #{tenantId}
          AND id = #{requestId}
          AND status = 'APPROVED'
    </update>
</mapper>
