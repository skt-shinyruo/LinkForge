<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkforge.platform.infrastructure.persistence.mapper.ApplicationPolicyMapper">

    <insert id="insert" parameterType="com.linkforge.platform.infrastructure.persistence.entity.ApplicationPolicyEntity">
        INSERT INTO application_policies (
            application_id,
            default_domain_scope,
            default_redirect_status_code,
            preview_enabled
        ) VALUES (
            #{applicationId},
            #{defaultDomainScope},
            #{defaultRedirectStatusCode},
            #{previewEnabled}
        )
    </insert>
</mapper>
