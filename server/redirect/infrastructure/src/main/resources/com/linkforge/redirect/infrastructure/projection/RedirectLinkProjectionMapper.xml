<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkforge.redirect.infrastructure.projection.RedirectLinkProjectionMapper">

    <resultMap id="RedirectLinkProjectionResultMap" type="com.linkforge.redirect.infrastructure.projection.RedirectLinkProjection">
        <id property="code" column="code"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="linkId" column="link_id"/>
        <result property="originalUrl" column="original_url"/>
        <result property="enabled" column="enabled"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="redirectStatusCode" column="redirect_status_code"/>
        <result property="previewEnabled" column="preview_enabled"/>
        <result property="unavailableLandingUrl" column="unavailable_landing_url"/>
        <result property="queryForwardMode" column="query_forward_mode"/>
        <result property="queryForwardAllowlist" column="query_forward_allowlist"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="projectionColumns">
        code,
        tenant_id,
        link_id,
        original_url,
        enabled,
        expires_at,
        redirect_status_code,
        preview_enabled,
        unavailable_landing_url,
        query_forward_mode,
        query_forward_allowlist,
        updated_at
    </sql>

    <select id="findByCode" resultMap="RedirectLinkProjectionResultMap">
        SELECT
        <include refid="projectionColumns"/>
        FROM redirect_link_projection
        WHERE code = #{param1}
        LIMIT 1
    </select>

    <insert id="upsert" parameterType="com.linkforge.redirect.infrastructure.projection.RedirectLinkProjection">
        INSERT INTO redirect_link_projection (
            code,
            tenant_id,
            link_id,
            original_url,
            enabled,
            expires_at,
            redirect_status_code,
            preview_enabled,
            unavailable_landing_url,
            query_forward_mode,
            query_forward_allowlist
        ) VALUES (
            #{code},
            #{tenantId},
            #{linkId},
            #{originalUrl},
            #{enabled},
            #{expiresAt},
            #{redirectStatusCode},
            #{previewEnabled},
            #{unavailableLandingUrl},
            #{queryForwardMode},
            #{queryForwardAllowlist}
        )
        ON DUPLICATE KEY UPDATE
            tenant_id = VALUES(tenant_id),
            link_id = VALUES(link_id),
            original_url = VALUES(original_url),
            enabled = VALUES(enabled),
            expires_at = VALUES(expires_at),
            redirect_status_code = VALUES(redirect_status_code),
            preview_enabled = VALUES(preview_enabled),
            unavailable_landing_url = VALUES(unavailable_landing_url),
            query_forward_mode = VALUES(query_forward_mode),
            query_forward_allowlist = VALUES(query_forward_allowlist),
            updated_at = NOW()
    </insert>

    <delete id="deleteByCode">
        DELETE FROM redirect_link_projection
        WHERE code = #{param1}
    </delete>
</mapper>

