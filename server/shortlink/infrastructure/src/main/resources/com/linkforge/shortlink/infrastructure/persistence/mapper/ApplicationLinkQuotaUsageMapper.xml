<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkforge.shortlink.infrastructure.persistence.mapper.ApplicationLinkQuotaUsageMapper">

    <!-- GET_LOCK/RELEASE_LOCK 必须由事务绑定的同一数据库连接执行；事务回滚不会自动释放该锁。 -->
    <select id="acquireMonthlyLinkUsageLock" resultType="int">
        SELECT GET_LOCK(#{lockName}, #{timeoutSeconds})
    </select>

    <select id="releaseMonthlyLinkUsageLock" resultType="int">
        SELECT RELEASE_LOCK(#{lockName})
    </select>

    <!--
        只在用量行首次出现时按 UTC 半开区间回填历史基线；调用方持有月份 named lock，
        主键与 INSERT IGNORE 则保证已建立的计数不会被重新覆盖。
    -->
    <insert id="ensureMonthlyLinkUsage">
        INSERT IGNORE INTO application_link_monthly_usages (
            tenant_id,
            application_id,
            month_start,
            used_count
        )
        SELECT
            #{tenantId},
            #{applicationId},
            #{monthStartUtc},
            COUNT(*)
        FROM short_links
        WHERE tenant_id = #{tenantId}
          AND application_id = #{applicationId}
          AND created_at &gt;= #{fromInclusiveUtc}
          AND created_at &lt; #{toExclusiveUtc}
    </insert>

    <!-- 将上限判断和递增放在同一 UPDATE 中，避免并发请求通过“先查询、后写入”超额预留。 -->
    <update id="incrementMonthlyLinkUsage">
        UPDATE application_link_monthly_usages
        SET used_count = used_count + 1
        WHERE tenant_id = #{tenantId}
          AND application_id = #{applicationId}
          AND month_start = #{monthStartUtc}
          AND used_count &lt; #{monthlyLinkLimit}
    </update>

</mapper>
