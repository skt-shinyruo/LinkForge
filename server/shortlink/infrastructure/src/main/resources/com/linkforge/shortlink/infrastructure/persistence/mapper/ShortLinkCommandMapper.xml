<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkforge.shortlink.infrastructure.persistence.mapper.ShortLinkCommandMapper">

    <insert id="insert" parameterType="com.linkforge.shortlink.infrastructure.persistence.entity.ShortLinkEntity">
        INSERT INTO short_links (
            id,
            tenant_id,
            application_id,
            domain_id,
            code,
            lifecycle_state,
            original_url,
            note,
            enabled,
            expires_at,
            archived_at,
            redirect_status_code,
            preview_enabled,
            unavailable_landing_url,
            query_forward_mode,
            query_forward_allowlist,
            created_by_type,
            created_by,
            version
        ) VALUES (
            #{id},
            #{tenantId},
            #{applicationId},
            #{domainId},
            #{code},
            #{lifecycleState},
            #{originalUrl},
            #{note},
            #{enabled},
            #{expiresAt},
            #{archivedAt},
            #{redirectStatusCode},
            #{previewEnabled},
            #{unavailableLandingUrl},
            #{queryForwardMode},
            #{queryForwardAllowlist},
            #{createdByType},
            #{createdBy},
            #{version}
        )
    </insert>

    <update id="update" parameterType="com.linkforge.shortlink.infrastructure.persistence.entity.ShortLinkEntity">
        UPDATE short_links
        SET original_url = #{originalUrl},
            note = #{note},
            enabled = #{enabled},
            lifecycle_state = #{lifecycleState},
            expires_at = #{expiresAt},
            archived_at = #{archivedAt},
            redirect_status_code = #{redirectStatusCode},
            preview_enabled = #{previewEnabled},
            unavailable_landing_url = #{unavailableLandingUrl},
            query_forward_mode = #{queryForwardMode},
            query_forward_allowlist = #{queryForwardAllowlist},
            version = version + 1
        WHERE tenant_id = #{tenantId}
          AND id = #{id}
          AND version = #{version}
    </update>

    <delete id="deleteByTenantIdAndIdAndVersion">
        DELETE FROM short_links
        WHERE tenant_id = #{param1}
          AND id = #{param2}
          AND version = #{param3}
    </delete>

    <update id="backfillOwnershipByTenant">
        UPDATE short_links
        SET application_id = #{param2},
            domain_id = #{param3}
        WHERE tenant_id = #{param1}
          AND application_id IS NULL
          AND domain_id IS NULL
    </update>
</mapper>
