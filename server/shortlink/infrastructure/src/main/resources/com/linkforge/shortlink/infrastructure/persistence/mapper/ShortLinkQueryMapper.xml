<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkforge.shortlink.infrastructure.persistence.mapper.ShortLinkQueryMapper">

    <resultMap id="ShortLinkResultMap" type="com.linkforge.shortlink.infrastructure.persistence.entity.ShortLinkEntity">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="applicationId" column="application_id"/>
        <result property="domainId" column="domain_id"/>
        <result property="hostname" column="hostname"/>
        <result property="code" column="code"/>
        <result property="lifecycleState" column="lifecycle_state"/>
        <result property="originalUrl" column="original_url"/>
        <result property="note" column="note"/>
        <result property="enabled" column="enabled"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="archivedAt" column="archived_at"/>
        <result property="redirectStatusCode" column="redirect_status_code"/>
        <result property="previewEnabled" column="preview_enabled"/>
        <result property="unavailableLandingUrl" column="unavailable_landing_url"/>
        <result property="queryForwardMode" column="query_forward_mode"/>
        <result property="queryForwardAllowlist" column="query_forward_allowlist"/>
        <result property="createdByType" column="created_by_type"/>
        <result property="createdBy" column="created_by"/>
        <result property="version" column="version"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="shortLinkColumns">
        id,
        tenant_id,
        application_id,
        domain_id,
        code,
        lifecycle_state,
        original_url,
        note,
        enabled,
        expires_at,
        archived_at,
        redirect_status_code,
        preview_enabled,
        unavailable_landing_url,
        query_forward_mode,
        query_forward_allowlist,
        created_by_type,
        created_by,
        version,
        created_at,
        updated_at
    </sql>

    <sql id="shortLinkColumnsAliased">
        sl.id AS id,
        sl.tenant_id AS tenant_id,
        sl.application_id AS application_id,
        sl.domain_id AS domain_id,
        sl.code AS code,
        sl.lifecycle_state AS lifecycle_state,
        sl.original_url AS original_url,
        sl.note AS note,
        sl.enabled AS enabled,
        sl.expires_at AS expires_at,
        sl.archived_at AS archived_at,
        sl.redirect_status_code AS redirect_status_code,
        sl.preview_enabled AS preview_enabled,
        sl.unavailable_landing_url AS unavailable_landing_url,
        sl.query_forward_mode AS query_forward_mode,
        sl.query_forward_allowlist AS query_forward_allowlist,
        sl.created_by_type AS created_by_type,
        sl.created_by AS created_by,
        sl.version AS version,
        sl.created_at AS created_at,
        sl.updated_at AS updated_at
    </sql>

    <select id="findByTenantIdAndId" resultMap="ShortLinkResultMap">
        SELECT
        <include refid="shortLinkColumns"/>
        FROM short_links
        WHERE tenant_id = #{param1}
          AND id = #{param2}
        LIMIT 1
    </select>

    <select id="findUnscopedByCode" resultMap="ShortLinkResultMap">
        SELECT
        <include refid="shortLinkColumns"/>
        FROM short_links
        WHERE code = #{param1}
          AND domain_id IS NULL
        LIMIT 1
    </select>

    <select id="findByDomainIdAndCode" resultMap="ShortLinkResultMap">
        SELECT
        <include refid="shortLinkColumns"/>
        FROM short_links
        WHERE domain_id = #{domainId}
          AND code = #{code}
        LIMIT 1
    </select>

    <select id="findActiveByCode" resultMap="ShortLinkResultMap">
        SELECT
        <include refid="shortLinkColumns"/>
        FROM short_links
        WHERE code = #{param1}
          AND archived_at IS NULL
        LIMIT 1
    </select>

    <select id="findActiveUnscopedByCode" resultMap="ShortLinkResultMap">
        SELECT
        <include refid="shortLinkColumns"/>
        FROM short_links
        WHERE code = #{param1}
          AND domain_id IS NULL
          AND archived_at IS NULL
        LIMIT 1
    </select>

    <!-- Active 在此仅约束域名状态并排除归档；短链 enabled/expiry/lifecycle 由 Redirect 统一判定。 -->
    <select id="findActiveByHostnameAndCode" resultMap="ShortLinkResultMap">
        SELECT
        <include refid="shortLinkColumnsAliased"/>,
        d.hostname AS hostname
        FROM short_links sl
        JOIN domains d ON d.id = sl.domain_id
        WHERE d.hostname = #{hostname}
          AND d.status = 'ACTIVE'
          AND sl.code = #{code}
          AND sl.archived_at IS NULL
        LIMIT 1
    </select>

    <select id="findActiveByLegacyBaseHostAndCode" resultMap="ShortLinkResultMap">
        SELECT
        <include refid="shortLinkColumnsAliased"/>,
        d.hostname AS hostname
        FROM short_links sl
        JOIN domains d ON d.id = sl.domain_id
        WHERE d.hostname = CONCAT('legacy-', sl.tenant_id, '.', #{baseHost})
          AND d.status = 'ACTIVE'
          AND sl.code = #{code}
          AND sl.archived_at IS NULL
        LIMIT 1
    </select>

    <select id="listByTenantIdAndIds" resultMap="ShortLinkResultMap">
        SELECT
        <include refid="shortLinkColumnsAliased"/>,
        d.hostname AS hostname
        FROM short_links sl
        LEFT JOIN domains d ON d.id = sl.domain_id AND d.tenant_id = sl.tenant_id
        WHERE sl.tenant_id = #{tenantId}
          AND sl.id IN
          <foreach collection="ids" item="id" open="(" separator="," close=")">
              #{id}
          </foreach>
        ORDER BY sl.id ASC
    </select>

    <select id="listIdsByTenantIdAndApplicationId" resultType="long">
        SELECT id
        FROM short_links
        WHERE tenant_id = #{tenantId}
          AND application_id = #{applicationId}
        ORDER BY id ASC
    </select>

    <select id="listIdsByTenantIdAndDomainId" resultType="long">
        SELECT id
        FROM short_links
        WHERE tenant_id = #{tenantId}
          AND domain_id = #{domainId}
        ORDER BY id ASC
    </select>

    <select id="countCreatedByTenantIdAndApplicationIdAndCreatedAtRange" resultType="long">
        SELECT COUNT(*)
        FROM short_links
        WHERE tenant_id = #{tenantId}
          AND application_id = #{applicationId}
          AND created_at &gt;= #{fromInclusiveUtc}
          AND created_at &lt; #{toExclusiveUtc}
    </select>

    <!-- count 与分页列表共享该过滤片段，修改筛选规则时必须保持两种查询口径一致。 -->
    <sql id="searchWhere">
        <where>
            tenant_id = #{tenantId}

            <choose>
                <when test="archived">
                    AND archived_at IS NOT NULL
                </when>
                <otherwise>
                    AND archived_at IS NULL
                </otherwise>
            </choose>

            <if test="enabled != null">
                AND enabled = #{enabled}
            </if>

            <if test="keyword != null and keyword != ''">
                AND (
                    LOWER(code) LIKE CONCAT('%', LOWER(#{keyword}), '%')
                    OR LOWER(original_url) LIKE CONCAT('%', LOWER(#{keyword}), '%')
                    OR LOWER(note) LIKE CONCAT('%', LOWER(#{keyword}), '%')
                )
            </if>

            <if test="tag != null and tag != ''">
                AND EXISTS (
                    SELECT 1
                    FROM link_tags lt
                    JOIN tags t ON t.id = lt.tag_id
                    WHERE lt.link_id = short_links.id
                      AND t.tenant_id = short_links.tenant_id
                      AND t.name = #{tag}
                )
            </if>

            <if test="applicationId != null">
                AND application_id = #{applicationId}
            </if>

            <if test="unscopedOnly">
                AND application_id IS NULL
            </if>

            <if test="createdBy != null">
                AND created_by = #{createdBy}
            </if>

            <if test="createdByType != null and createdByType != ''">
                AND created_by_type = #{createdByType}
            </if>
        </where>
    </sql>

    <select id="countSearch" resultType="long">
        SELECT COUNT(*)
        FROM short_links
        <include refid="searchWhere"/>
    </select>

    <select id="listSearch" resultMap="ShortLinkResultMap">
        SELECT
        <include refid="shortLinkColumns"/>
        FROM short_links
        <include refid="searchWhere"/>
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
